{% if rest %}
<header class="hero" style="background-image: url(../img/{{ hero }})">
  <div class="box">
    <h1 class="hero-h1">
      {{ h1 }}
    </h1>
    <p class="hero-p lead">{{ caption }}</p>
  </div>
  {% include lights %}
</header>
{% else %}
<header class="hero" style="background-image: url(./img/{{ hero }})">
  <div class="box">
    <h1 class="hero-h1">
      {{ h1 }}
    </h1>
    <p class="hero-p lead">{{ caption }}</p>
  </div>
  {% include lights %}
</header>
{% endif %}
